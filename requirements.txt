# LegalMind dependencies
# Python 3.11+ required

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
jinja2>=3.1.4
python-multipart>=0.0.9
sse-starlette>=2.1.0

# Configuration
pyyaml>=6.0.1

# PDF processing
pdfplumber>=0.11.0
pypdfium2>=4.25.0

# Retrieval — hybrid semantic + BM25
chromadb>=0.5.0
sentence-transformers>=3.0.0

# Local LLM inference (GGUF)
# NOTE: llama-cpp-python is NOT installed from this file — it needs a
# CUDA-enabled build. The Dockerfile builds it with CUDA support:
#   CMAKE_ARGS="-DGGML_CUDA=on" pip install --no-cache-dir --force-reinstall llama-cpp-python==0.3.20
# For manual/local setup (Windows PowerShell):
#   $env:CMAKE_ARGS="-DGGML_CUDA=on"; pip install --force-reinstall llama-cpp-python==0.3.20

# GPU monitoring
psutil>=5.9.0

# Token counting
tiktoken>=0.7.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
httpx>=0.27.0

# Utilities
filelock>=3.15.0
diskcache>=5.0

# Memory system
mempalace>=3.3.0
